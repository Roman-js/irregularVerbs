import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    linearGradient: {
        flex: 1,
        paddingTop: 50
    },
    flatListStyle: {
        width: '100%'
    },
    flatListContainer: {
        alignItems: 'center'
    }
})